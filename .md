## 第二次提交
完成了文档解析和加载，现在支持多文档解析和加载
## 修复跨库同步一致性与检索/入口问题，恢复被回滚的优化项提交
P0 一致性：
- retry 重放不完整：更新中途 Milvus 同步失败会残留旧 chunk 孤儿向量且重跑无法清理；
  retry 改为按 source 删除该文件全部向量再全量插入（单文件幂等重建）。
  milvus_store 新增 _source_expr / delete_chunks_by_source，
  ingest 的 _sync_milvus 增加 rebuild_source 参数。
- source 口径脆弱：process_file 中 metadata.source 由 setdefault 改为强制覆盖为 file_path，
  保证 MySQL 指纹与 Milvus chunk 确定性主键口径完全一致，避免更新删除、幂等、检索过滤失效。
- document_validation_exception：去掉错误码常量行尾逗号（误写成元组）。

恢复被回滚的优化：
- 检索过滤与阈值配置：search / search_with_rerank 支持 expr、source 过滤（Milvus 服务端过滤），
  RERANK_SCORE_THRESHOLD 改为从环境变量读取（默认 0.3）。
- 异步包装：新增 aadd_chunks / adelete_chunks_by_ids / adelete_chunks_by_source /
  asearch_with_rerank（asyncio.to_thread），ingest 的 Milvus 同步全部改为 await 异步版本。
- 入口拆分：main.py 拆分为 ingest（入库/增量同步）与 search（向量召回 + rerank 精排）两个子命令。

健壮性：
- load_file：修复 load_txt 的 GBK 回退无效（TextLoader 构造不读文件，解码异常在 load() 才抛，
  try 现包住 smart_load，UTF-8 解码失败正确回退 GBK）。
- db_config：SQL 日志由硬编码 echo=True 改为环境变量 DB_ECHO 控制（默认关闭，调试时 .env 开启）。